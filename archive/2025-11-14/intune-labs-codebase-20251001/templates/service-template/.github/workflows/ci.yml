name: CI
on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - main
jobs:
  call-org-ci:
    # For multi-language repos, wrap this job in a matrix and forward matrix.language.
    uses: your-org/.github/.github/workflows/reusable-ci.yml@main
    with:
      language: node
