# Infrastructure

Place Terraform or Pulumi definitions here. The baseline repo expects:
- Shared modules under `modules/`
- Environment configs under `env/`

Use GitHub Actions OIDC for cloud authentication and keep secrets in your cloud provider.
